let objects = [];


for(let i=0; i< QText.length ; i++){
    objects.push({});
    objects[i].text = QText[i];
    objects[i].location = locations.arr[i];
    objects[i].imageUrl = imageUrl.arr[i];
}

